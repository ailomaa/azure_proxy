# Azure credentials
AZURE_KEY=
AZURE_REGION=

# Phoenix runtime secret — generate with: openssl rand -base64 64
SECRET_KEY_BASE=

# Server binding (host used in URL generation, port used inside container and mapped to host)
PHX_HOST=localhost
PORT=4000
